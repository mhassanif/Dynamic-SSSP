#include "../io/graph_io.h"
#include "../partition/metis_partition.h"
#include "../update_engine/update_engine.h"

int main(int argc, char **argv)
{
    // TODO: Parse CLI args (e.g., --partition, --update, --run)
    // Use other modules to orchestrate the pipeline
    return 0;
}
