#include "update_engine.h"

void apply_edge_updates(GraphPartition &g,
                        const std::vector<std::tuple<int, int, int>> &insertions,
                        const std::vector<std::pair<int, int>> &deletions)
{
    // TODO: Apply updates
}
